// Band C - MC 1.21.1 (primary / TBA target)
plugins {
    id "fabric-loom"
}

ext.slashloot = [ids: "location", vehicle: "legacy", store: "nbt", open: "player"]

apply from: "${rootDir}/gradle/fabric-band.gradle"
